<Window x:Class="SimRacingSdk.Lmu.Demo.ResultExplorer.ResultExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimRacingSdk.Lmu.Demo.ResultExplorer"
        mc:Ignorable="d"
        Title="Result Explorer"
        Height="900"
        Width="1200"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="LightGray"
        d:DataConcontent="{d:DesignInstance local:ResultExplorerViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Grid.Column="0"
               Grid.Row="0"
               Content="Sessions"
               HorizontalAlignment="Center"
               VerticalAlignment="Center" />
        <ListBox Grid.Column="0"
                 Grid.Row="1"
                 ItemsSource="{Binding ResultFiles}"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Stretch"
                 SelectedItem="{Binding SelectedResult, Mode=TwoWay}"
                 MinWidth="150"
                 DisplayMemberPath="FileName" />

        <Label Grid.Column="1"
               Grid.Row="0"
               Content="Session Details"
               HorizontalAlignment="Center"
               VerticalAlignment="Center" />
        <Grid Column="1"
              Row="1" Margin="10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Recorded" FontWeight="Bold"/>
            <Label Grid.Column="1" Grid.Row="0" Content="{Binding SessionDateTime}" />

            <Label Grid.Column="0"
                   Grid.Row="1"
                   Content="Track Venue"
                   FontWeight="Bold" />
            <Label Grid.Column="1"
                       Grid.Row="1"
                       Content="{Binding TrackVenue}" />

            <Label Grid.Column="2"
                   Grid.Row="1"
                   Content="Track Course"
                   FontWeight="Bold" />
            <Label Grid.Column="3"
                       Grid.Row="1"
                       Content="{Binding TrackCourse}" />

            <Label Grid.Column="0"
                   Grid.Row="2"
                   Content="Track Event"
                   FontWeight="Bold" />
            <Label Grid.Column="1"
                   Grid.Row="2"
                   Content="{Binding TrackEvent}" />

            <Label Grid.Column="2"
                   Grid.Row="2"
                   Content="Track Length (M)"
                   FontWeight="Bold" />
            <Label Grid.Column="3"
                   Grid.Row="2"
                   Content="{Binding TrackLength}" />

            <GroupBox Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="4" Header="Settings">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Label Grid.Column="0"
                           Grid.Row="0"
                           Content="Client Fuel Visible"
                           FontWeight="Bold" />
                    <CheckBox Grid.Column="1"
                           Grid.Row="1"
                           Content="{Binding ClientFuelVisible}" />
                </Grid>
            </GroupBox>

        </Grid>


        </Grid>
</Window>