<Window x:Class="SimRacingSdk.Acc.Demo.CarExplorer.CarExplorerWindow"        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimRacingSdk.Acc.Demo.CarExplorer"
        mc:Ignorable="d"
        Title="Car Explorer"
        Height="650"
        Width="900"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        d:DataContext="{d:DesignInstance local:CarExplorerViewModel}">
    <Window.InputBindings>
        <KeyBinding Gesture="CTRL+SHIFT+ALT+C"
                    Command="{Binding ExportCsvCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ListBox Grid.Column="0"
                 Grid.Row="0"
                 Grid.RowSpan="2"
                 ItemsSource="{Binding CarClasses}"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Stretch"
                 SelectedItem="{Binding SelectedClass, Mode=TwoWay}"
                 MinWidth="100"/>

        <DataGrid Grid.Column="1"
                  Grid.Row="0"
                  ItemsSource="{Binding Cars}"
                  SelectedItem="{Binding SelectedCar, Mode=TwoWay}"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Stretch"
                  Height="200"
                  AutoGenerateColumns="False"
                  Margin="5"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Manufacturer"
                                    Binding="{Binding ManufacturerTag}" />
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding DisplayName}" />
                <DataGridTextColumn Header="Year"
                                    Binding="{Binding Year}" />
            </DataGrid.Columns>
        </DataGrid>
        <GroupBox Grid.Column="1"
                  Grid.Row="1"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Header="Car Details"
                  DataContext="{Binding SelectedCar}"
                  Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Image Grid.Column="0"
                       Grid.Row="0"
                       Source="{Binding ManufacturerTag, Converter={StaticResource CarManufacturerImageSourceConverter}}"
                       Height="64" />
                <Label Grid.Column="1"
                       Grid.Row="0"
                       FontWeight="Bold"
                       FontSize="16"
                       Content="{Binding DisplayName}"
                       VerticalAlignment="Center" />

                <Label Grid.Column="0"
                       Grid.Row="1"
                       Content="Year"
                       HorizontalAlignment="Left"
                       FontWeight="SemiBold" />
                <Label Grid.Column="1"
                       Grid.Row="1"
                       Content="{Binding Year}"
                       HorizontalAlignment="Left" />

                <Label Grid.Column="0"
                       Grid.Row="2"
                       Content="Max Fuel"
                       HorizontalAlignment="Left"
                       FontWeight="SemiBold" />
                <Label Grid.Column="1"
                       Grid.Row="2"
                       Content="{Binding MaxFuel}"
                       HorizontalAlignment="Left" />

                <Label Grid.Column="0"
                       Grid.Row="3"
                       Content="Max RPM"
                       HorizontalAlignment="Left"
                       FontWeight="SemiBold" />
                <Label Grid.Column="1"
                       Grid.Row="3"
                       Content="{Binding MaxRpm}"
                       HorizontalAlignment="Left" />
            </Grid>
        </GroupBox>
    </Grid>
</Window>